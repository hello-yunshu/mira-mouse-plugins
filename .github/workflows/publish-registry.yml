# SPDX-License-Identifier: CC0-1.0
name: Publish Plugin Registry
on:
  release:
    types: [published]
permissions:
  contents: write
concurrency:
  group: plugin-registry
  cancel-in-progress: false
jobs:
  publish:
    if: startsWith(github.event.release.tag_name, 'plugin/')
    runs-on: ubuntu-22.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with: { ref: main }
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with: { node-version: 22 }
      - name: Download and verify published package
        env:
          GH_TOKEN: '${{ github.token }}'
          TAG: '${{ github.event.release.tag_name }}'
          REPO: '${{ github.repository }}'
        run: |
          set -euo pipefail
          mkdir release-asset
          gh release download "$TAG" --repo "$REPO" --dir release-asset --pattern '*.mira-plugin' --pattern '*.mira-plugin.sha256'
          asset="$(find release-asset -name '*.mira-plugin' -type f -print -quit)"
          checksum="$asset.sha256"
          test -f "$asset" && test -f "$checksum"
          expected="$(tr -d '[:space:]' < "$checksum")"
          actual="$(sha256sum "$asset" | awk '{print $1}')"
          test "$expected" = "$actual"
          unzip -p "$asset" plugin.json > release-asset/plugin.json
          asset_name="$(basename "$asset")"
          asset_url="$(gh api "repos/$REPO/releases/tags/$TAG" --jq ".assets[] | select(.name == \"$asset_name\") | .browser_download_url")"
          test -n "$asset_url"
          echo "ASSET_URL=$asset_url" >> "$GITHUB_ENV"
          echo "SHA256=$actual" >> "$GITHUB_ENV"
      - name: Update registry
        env:
          RELEASE_TAG: '${{ github.event.release.tag_name }}'
        run: node scripts/update-registry.mjs registry/index.json release-asset/plugin.json
      - run: npm run validate && npm test
      - name: Commit immutable registry metadata
        env:
          TAG: '${{ github.event.release.tag_name }}'
        run: |
          set -euo pipefail
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add registry/index.json
          git diff --cached --quiet && exit 0
          git commit -m "chore(registry): publish $TAG"
          git push origin HEAD:main
