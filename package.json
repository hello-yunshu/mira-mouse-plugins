{
  "name": "mira-mouse-plugins",
  "version": "1.0.0",
  "private": true,
  "description": "Declarative device plugins for Mira",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "test": "node --test tests/*.test.mjs",
    "pack": "node scripts/pack-sign.mjs",
    "sync:hidpp": "node scripts/sync-hidpp-features.mjs",
    "sync:hidpp:check": "node scripts/sync-hidpp-features.mjs --check"
  },
  "license": "AGPL-3.0-or-later",
  "devDependencies": {}
}
