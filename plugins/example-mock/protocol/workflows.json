{
  "schemaVersion": 1,
  "workflows": {
    "virtual-wireless-mouse-read": {
      "transport": "mock",
      "steps": [
        { "command": "snapshot", "parser": "snapshot", "output": "snapshot" }
      ]
    },
    "snapshot": {
      "transport": "mock",
      "steps": [
        { "command": "snapshot", "parser": "snapshot", "output": "snapshot" }
      ]
    }
  }
}
